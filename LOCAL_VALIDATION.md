# 单模型调度与 SVD 回退：本地验证结果

2026-09-10。开发分支 `scheduler/single-model-queue`，起点
`6a237f2cf1858e0b3716aad60e94b88e35860fb2`。这是独立开发版本；当前 GPA
生产源码、作业和控制器未被本次开发修改。用户已授权开发，并明确要求加入 SVD 备选算法。

## 实测结论

相同三个物理核下，单模型动态派工在这组本地 GPA 测试中将整体用时缩短 **13.6%**。
共享缓存可以安全复用外层准备，但这次没有观察到额外整体收益。因此 worker 默认关闭跨任务
缓存，保留 `--memory-cache-mib` 和 `--node-cache` 选项供特定工作负载验证后启用。

| 方案 | 第 1 轮 / 秒 | 第 2 轮 / 秒 | 平均 / 秒 | worker 执行任务的时间占比 |
|---|---:|---:|---:|---:|
| 每个 cell 七模型固定一组，静态分配 | 67.921 | 67.169 | 67.545 | 84.58% |
| 单模型动态队列 | 58.563 | 58.163 | 58.363 | 98.91% |
| 单模型动态队列 + RAM/节点共享缓存 | 58.008 | 59.494 | 58.751 | 99.08% |

动态队列相对固定分组吞吐约为 1.157 倍；加缓存后的平均时间比无缓存长约 0.67%，
两轮样本不足以判断这一小差异。上表占比为累计任务墙钟时间 / 预留核秒，**不是 CPU 利用率**。
平均模型任务 CPU 秒分别为 166.86、170.14、170.65：主要收益来自减少等待最慢一组结束时
的空闲，未显示模型计算量本身明显下降。

测试条件：

- GPA 原始实际数据；seed=12345、draw=0；四个 `(N,K)` 为 `(122,47)`、`(122,400)`、
  `(907,261)`、`(122,261)`，七个需填补缺失值的模型均使用原完整训练/CV预算。
- 三个常驻独立进程各绑定一个物理核；模型、BLAS/OpenMP 单线程。两轮顺序互逆。
  所有方案使用同样的 Windows 进程内数值执行路径。
- 动态方案计入真实 loopback HTTP、单模型结果日志的 fsync、结果暂存和确认。
  固定方案计入每组完成后的 fsync。初始化/加载/热身各约 43–45 秒单独记录，不计入表中。
- 每轮 28 个模型格点，六轮共 **168 次正式运行**，全部成功，跨方案预测逐位一致。
  其中 **42 次**额外逐位对照之前保存的原生产算法版本 N122/K400 预测。
- 缓存方案每进程最多保留 256 MiB，节点缓存上限 1 GiB；实际每轮发布四个 cell，约 61.20 MiB。
  RAM限制指缓存保留量，不是进程总内存上限；准备/模型训练还有临时内存。
- 第一轮固定分组的内存峰值受进程状态影响，第二轮明显降低，不能据此宣称队列降低了内存。

这些是四个格点的本地结果；未覆盖最大网格、多 seed、Linux 原生子进程、Lustre、跨节点通信和
Slurm 开销。不能直接外推完整生产实验的加速比例。

## 新调度的具体行为

每个逻辑任务严格为 `(seed, draw, N, K, model)`，一核同时执行一个模型任务。空闲 worker
领取下一项，完成一项就持久化并确认一项。相同 token 和结果的重发可重复确认，过期租约不能
覆盖已接受的结果。租约丢失后允许重复计算，但结果只接受一次。基础设施重试有次数上限；数值
失败显式留下失败状态，不将其误报为实验完成。

初始成本优先级使用 `模型权重 × N × sqrt(K)`，权重与本次基准一致；这是可覆盖的初始启发式，
不是已经拟合并验证的全网格耗时预测。相近成本任务间可偏好当前 worker 已缓存的 cell。

服务可指定本地 `--drain-file`：创建文件后停止新领取，当前模型仍可心跳和提交；移除文件后
恢复领取。暂停状态记入日志。服务默认绑定本机，工作凭证没有导入旧结果或管理暂停的远程权限。

缓存仅包含原始切片、目标和外层诊断准备，不缓存任何 CV 拟合状态。模型仍拿原始缺失值输入，
在各自训练折内拟合预处理器。共享只读缓存有内容校验、命名空间隔离和磁盘准入限制；崩溃遗留的
未发布文件会在持锁恢复时清理，已发布映射不在读者仍运行时删除。全量生产还需要节点生命周期
与缓存轮换接线。

## SVD 备选算法

`FoldLocalRidge` 先执行原 `numpy.linalg.svd`。只有捕获 `LinAlgError` 且输入矩阵全部有限时，
才调用 `scipy.linalg.svd(lapack_driver="gesvd")`。不改矩阵、alpha 网格、CV 划分、rank cutoff、
标准化或最终拟合算法。正常路径的返回值直接保留。备选仍失败就继续报告失败，不换成伪成功结果。

独立 Ridge 与 Super Learner 内部的 Ridge 共用此实现。真实触发时写日志，已拟合 Ridge 保存
`svd_fallback_count_`。三个模型参数文件仅将算法标识更新为
`nk-grid-v10-svd-gesvd-fallback-1`，未调整超参数。

本地验证覆盖正常路径逐位一致、强制不收敛后的矩阵重构、非有限输入拒绝回退、无关异常不吞掉、
备选仍失败时抛错、真实 Ridge CV 与完整 SL 组装中的嵌套 Ridge 回退。
SL 这个集成夹具缩短了训练预算，因此产生 6 个预期 MLP 收敛警告；它不是性能基准，生产参数未缩短。
实际 GPA 基准仍使用完整参数。

原实验另有 16 条历史 SVD 失败在隔离环境验证过 gesvd 可恢复，证据保存在
`runs/ffc-gpa-production-20260909/SVD_DIAGNOSIS_20260910.md` 和
`NEW_SVD_DIAGNOSIS_20260910.md`。本次未重跑这些远端作业，亦未把诊断结果导入生产。
第 17 条新失败不能算入已验证的 16 条。

## 恢复与兼容性验证

**35 项本地检查通过**，包含：并发领取、领取/提交响应丢失、心跳暂时不可用、过期与重启隔离、
持久化后索引更新前崩溃、日志尾部截断、已提交日志篡改拒绝、暂停续跑、数值失败状态、共享缓存
只读/校验/容量/LRU与发布崩溃、旧分组成功模型独立导入、冲突/不兼容数据拒绝、SVD、实际 planner CLI。

旧结果导入先校验完整导出包，再执行导入；保留每条旧记录的来源与旧算法标识，旧失败留作待算。
兼容性证书逐文件核对全部原有 Python 模块，只允许已审查的 Ridge 回退替换；YAML 只允许算法
版本字段变化。证书绑定参数文件哈希变化，其他科学设置/数据变化仍会拒绝。
导出适配器的接口和旧 JSON 编码已做本地夹具检查；真正的 POSIX 封存、WAL 锁与导入续跑仍未验证。

额外本地规模探针：10 万个逻辑任务，16 个 HTTP 客户端，2,000 个合成短结果。

| 操作 | 实测 |
|---|---:|
| 流式任务清单写出 | 0.673 秒 |
| 本地 SQLite 索引重建 | 9.143 秒 |
| 2,000 次领取 + 结果持久化确认 | 5.620 秒（约 356 项/秒） |
| 重启、重建、日志重放 | 10.838 秒 |
| 重启核对 | 已完成 2,000；待执行 98,000，完全一致 |
| 带结果索引大小 | 69,005,312 字节 |

该探针使用小型合成结果与本地磁盘，不能证明 1,800 万任务、百万级旧结果导入或集群文件系统
下的吞吐。当前实现每次启动重建索引，生产切换前必须测量完整规模并评估快照/压缩需求。

## 产物和下一阶段

外层 `scheduler-development-20260910` 目录保存完整结果：`gpa-benchmark-01/`、
`queue-scale-100k/report.json`、`checks-final.log`、`validation-summary.json`、
`compatibility-certificate.json`、`development-source.zip`。
GPA 基准保存运行时源码哈希；此后修改了通信故障恢复、导入校验、缓存发布恢复和 CLI，
数值实现未再变化。规模探针也保存了当时源码哈希；最后新增本地 drain 服务循环有单独测试。

本阶段完成独立实现与上述本地验证；**尚未接入生产 Slurm 调度，也未切换当前实验**。
正式切换前还需：核实 XGBoost/LightGBM 完整有效覆盖；通过 Linux 原生 worker 与真实
封存/导入/续跑；验证完整规模、节点连接、持久化吞吐和回收；停妥旧运行的自动续跑控制器，
再封存导入并用小批恢复任务验收。具体边界见 `SCHEDULER_DEVELOPMENT.md`。
