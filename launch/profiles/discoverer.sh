# Discoverer CPU cluster. Accounts are supplied explicitly at launch.
# Override only when an alternative module has been tested on compute nodes.
export PYTHON_MODULE="${DISCOVERER_PYTHON_MODULE:-python/3/3.12/3.12.4}"
