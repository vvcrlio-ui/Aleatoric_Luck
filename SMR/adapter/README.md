# SMR adapter

This is intentionally a near-identity adapter: it projects the declared
outcomes and the fixed `Aset`/`Bset` feature universe without fitting,
imputing, splitting or sampling.

```bash
python adapter/prepare_smr.py \
  --source NK_Grid/data/asample2_withlag.csv
```
