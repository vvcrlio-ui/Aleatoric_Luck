# 698 worker 的任务耗时均衡

2026-09-10 用户最新澄清：服务器已有698个计算worker，关注不同任务/worker之间的耗时均衡、
缩短整轮尾部等待；不要把重点转成本机4核/8核配置比较。四类队列不意味着固定分核。
已停止扩展本机并发数实验；`queue_concurrency_benchmark.py` 没有执行。

## 当前生产证据

使用已经下载的两次完整前缀审计，没有访问或修改运行中的生产文件。
最新前缀时间为2026-09-10 21:06:51 UTC（巴黎23:06:51），比较前一小时20:06:40 UTC。

| 原生产任务组 | 原分配worker | 已做完自己全部任务 | 仍有自己的任务 | 剩余任务组 |
|---|---:|---:|---:|---:|
| XGBoost + LightGBM | 349 | 210 | 139 | 30,393 |
| 其余七模型 | 349 | 0 | 349 | 517,815 |

18,000,000项预期模型结果中已有效14,314,490项，仍缺少或失败3,685,510项。
210/698≈30.1%的原worker已经完成固定清单，无法接手另一个worker的剩余任务。
这是本实验失去的worker处理能力，不代表整台服务器的物理CPU都空闲，也不是CPU利用率测量。

旧分配的两类任务交替排列，再按worker数698取模，导致349个worker固定承担boosting组，
另349个固定承担七模型组。模型组之间的工作量差异未被任务数量均分消除。

仅按模型名称判断任务长短也不够：在最新这一小时、两次快照均未做完分配的worker中，
“该worker的时间间隔/完成组数”的中位值，boosting双模型组约212.4秒，七模型组约106.2秒。
这是不同N/K阶段的区间平均吞吐指标，不是单个模型耗时或逐任务耗时分布。
不能把boosting永久标为短任务，亦不能用这些均值直接预测后面的全量完成时间。

证据文件位于 `runs/ffc-gpa-production-20260909/full-audit-20260910T210700Z/` 和
`full-audit-20260910T200630Z/`。派生结果为本开发目录外层 `balance-698/snapshot-analysis.json`。

## 应采用的调度目标

698个worker共同消费剩余任务；不为某类保留做完后无法转用的固定worker。
四类可以保留为成本估计、内存需求和观测统计的标签，领取仍以一个模型结果为单位。

1. **按预计耗时排序，而不是均分任务个数。** 用 `(model,N,K_expanded)` 及已有实际完成记录
   建立成本估计，区分同类中的大小格点；用新完成任务不断校准。不能仅凭类别四等分，也不能将
   当前初始 `模型权重*N*sqrt(K)` 启发式视为最终生产耗时模型。
2. **长任务尽早进入698核的并行池。** SL、NN以及大N/K下的Ridge、Lasso、boosting都可能进入
   长任务优先序列，避免最后只留下少数长任务。成本相近时再偏好可复用输入的格点。
3. **短任务连续补位，独立确认。** worker做完一项立即领取下一项；可连续接同格点的若干短模型
   以复用输入，但不把五模型/两模型捆成必须整体完成和整体重算的大任务，也不提前囤积长清单。
4. **四类之间随时调配资源。** 基于每类未完成的预计累计耗时调整领取优先级，全部worker可跨类；
   boosting队列清空后，全部worker均可服务其余三类。
5. **明确单模型耗时的边界。** 单个模型训练已经很长时，分类和换队列不会缩短这次计算。
   如果确实需要降低单项不可拆分的最长时间，下一层才是把CV折/候选拟合拆成内部子任务再汇总，
   并验证折划分、随机数、alpha选择、OOF预测和最终预测保持一致。不能通过强制超时重跑来“缩短”任务。

后续验收以698个worker的生产任务清单和耗时分布为依据，比较预计负载的最大值/分位数、
worker空闲核时、最后一批任务的尾部时间、结果持久化和调度开销。已有审计保存了完整结果计数，
但公共结果CSV没有保留逐模型 `_fit_seconds`/`_cell_wall_seconds`，因此本次没有伪造一次
覆盖全部生产任务的精确耗时回放，也不报告未经测量的698核加速百分比。

## 已结束的本地分类实验：仅保留机制证据

在用户澄清前已启动的四核分类实验正常结束，共288次完整预算的九模型拟合，四个GPA格点、
两轮逆序，预测跨方案逐位一致；其中224次七模型结果核对此前保存的预测哈希。
原始数据 `four-class-benchmark-01/`。它验证本地分类路由和跨类领取能够运行，不能代表698核
全量收益。分类router只是小清单测试适配器，尚未接入生产。

当前生产调度、任务、结果和控制器均未因此改变；原先的SVD条件回退与开发阶段验证仍保留。
正式切换仍遵循已约定的boosting完成、Linux/规模恢复验收和旧运行封存流程。
